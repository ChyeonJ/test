package exam;

public class Method_Q1 {
	
	//1번. 1부터 파라미터로 전달된 숫자까지의 합을 구한다.
	// printSum(10) => 55
	public static int addPlus (int num) {
		int hap = 0;
		for(int i = 1; i <= num; i++) {
			hap += i;
		}
		return hap;
	}
	
//	//2번
//	public static int printPrime(int numPrime) {
//		boolean check = false;
//		for (int i = 4; i <= numPrime; i++) {
//			check = false;
//			for(int j = 4; j < i; j++) {
//				if (numPrime % 4 == 0) {
//					check = true;
//				}
//			}
//		}
//		if(check == false) {
//			return numPrime;
//		}
//	}
	
	//3번. 파라미터로 전될된 숫자 두개 중 가장 큰 수 하나만 출력한다.
	public static int printMax(int num1, int num2) {
		
		int resultMax = Math.max(num1, num2);
		return resultMax;
		
	}
	
	//4번. 파라미터로 전달된 나이가 성인인지 미성년인지 구분하여 출력한다.
	public static String printAge(int age) {
		String resultStr = " ";
		if (age > 19) {
			resultStr = "성인입니다.";
		}
		else if (age <= 19) {
			resultStr = "미성년자입니다.";
		}
		return resultStr;
	}
	
	// 5번 랜덤한 숫자를 생성하고 아래 규칙에 따른 결과를 반환한다.
	
	public static String getFizzBuzz(int num) {
		String resultFizzBuzz = "";
		if(num % 3 == 0 && num % 5 == 0) {
			resultFizzBuzz = "FizzBuzz";
		}
		else if ( num % 3 == 0) {
			resultFizzBuzz = "Fizz";
		}
		else if(num % 5 == 0){
			resultFizzBuzz = "Buzz";
		}
		else {
			resultFizzBuzz = "" + num;
		}
		
		return resultFizzBuzz;
	}
	
	public static void main(String [] args) {
		
		int random = (int) (Math.random() * 100);
		
		int result = addPlus(10);
		System.out.println(result);
		
		result = printMax(22, 333333);
		System.out.println(result);
		
		String resultAge = printAge(20);
		System.out.println(resultAge);
		
		String resultFizzBuzz = getFizzBuzz(15);
		System.out.println(resultFizzBuzz);
		

		
		
		
	}

}
