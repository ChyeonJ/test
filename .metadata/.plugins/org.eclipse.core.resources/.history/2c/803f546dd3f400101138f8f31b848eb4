package exam;

public class Q14 {
	public static void main(String[] args) {
		// 3인 가족이 100만원으로 비행기를 타고 편도 여행을 가려합니다.
		// 부모님의 나이는 각각 40, 36세입니다.
		// 딸의 나이는 11세입니다.
		// 성인의 비행요금은 30만원
		// 아동의 비행요금은 12만원입니다. 성인 판단 기준은 19세 이상입니다.
		// 3인 가족은 여행을 떠날 수 있을까요?
		// 여행을 떠날 수 있다면 "여행가자!"
		// 여행을 떠날 수 없다면 "다음에가자"
		// 를 출력해보세요.
		
		//다중 if를 쓰라는건가...
		
		int money = 1_000_000;
		
		int father = 40;
		int mother = 36;
		int daugther = 11;
		
		int adultOneWayFlighFare = 300_000;
		int kidOneWayFlightFare = 120_000;
		
		int sum = 0;
		
		if (father >= 19 && mother >= 19) {
			sum = adultOneWayFlighFare * 2;
			
		}
		else if (daugther <= 19) {
			sum += kidOneWayFlightFare;
		}
		else if (daugther >= 19) {
			sum += adultOneWayFlighFare;
		}
		
		
	}
}
